var app = angular.module( 'COREAPI', [] );
app.controller( 'StampsController', function( $scope, $http, $window) {

	$scope.test = 'testejam';

	$scope.errorData = {
		error : false,
		message : 'error?'
	};



});
